# OpenVINO Streaming ASR - Requirements
# For edge deployment with INT4 quantization

# Core OpenVINO (use one of these version ranges based on your setup)
# For OpenVINO 2024.x:
openvino>=2024.0.0,<2025.0.0

# OpenVINO GenAI - for Whisper pipeline (optional but recommended)
# Install separately if needed: pip install openvino-genai
# openvino-genai>=2024.0.0

# OpenVINO tokenizers (optional - not required for basic functionality)  
# openvino-tokenizers>=2024.0.0

# NNCF for INT4 quantization
nncf>=2.7.0

# Optimum Intel (recommended for model conversion)
# Install with: pip install optimum-intel[openvino]
optimum-intel>=1.14.0

# Transformers and model loading
transformers>=4.36.0
torch>=2.0.0

# Audio processing
soundfile>=0.12.0
numpy>=1.24.0

# Web server and WebSocket
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-socketio>=5.10.0
aiohttp>=3.9.0
websockets>=12.0

# API models
pydantic>=2.5.0

# Logging
loguru>=0.7.0

# Dataset loading (for calibration)
datasets>=2.16.0

# Optional: Silero VAD
# silero-vad>=4.0.0

# Development dependencies (optional)
# pytest>=7.0.0
# pytest-asyncio>=0.23.0
# httpx>=0.26.0
